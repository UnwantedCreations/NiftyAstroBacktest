"""Nifty astro-backtest: an honest experiment.

We test whether planetary aspects carry ANY predictive signal for daily Nifty 50
moves, using a strict train/test split. The goal is to *measure*, not to assume.
A negative result is a valid, useful result.
"""
